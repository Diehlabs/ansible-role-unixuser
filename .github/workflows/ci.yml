name: Ansible CI

on:
  workflow_dispatch:
  push:
    paths-ignore:
      - '**.md'
  pull_request:
    paths-ignore:
      - '**.md'

jobs:
  default:
    uses: Diehlabs/shared-workflows/.github/workflows/ansible-ci.yml@v0.2.4

  more_vars:
    uses: Diehlabs/shared-workflows/.github/workflows/ansible-ci.yml@v0.2.4
    with:
      scenario: more_vars
